{
  "name": "compiling-ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node lib",
    "dev": "nodemon --exec ts-node lib",
    "build": "tsc --build && tsc --build src"
  },
  "nodemonConfig": {
    "watch": [
      "src/**/*"
    ],
    "ext": "html,css,ts",
    "delay": "0.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.6",
    "node-typescript-compiler": "^2.2.1",
    "ts-node": "^9.1.1"
  },
  "dependencies": {
    "@types/node": "^14.14.16",
    "typescript": "^4.1.3"
  }
}
